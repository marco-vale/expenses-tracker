query GetExpenses($options: QueryOptions) {
  expenses(options: $options) {
    expenses {
      id
      description
      type
      amount
      date
      category {
        id
        name
      }
    }
    count
  }
}
