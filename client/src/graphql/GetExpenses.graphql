query GetExpenses($filters: ExpensesFilters, $options: QueryOptions) {
  expenses(filters: $filters, options: $options) {
    expenses {
      id
      description
      type
      amount
      date
      category {
        id
        name
      }
    }
    count
  }
}
