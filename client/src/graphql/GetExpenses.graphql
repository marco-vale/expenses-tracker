query GetExpenses($options: ExpensesOptions) {
  expenses(options: $options) {
    expenses {
      id
      description
      type
      amount
      date
      category {
        id
        name
      }
    }
    count
  }
}
