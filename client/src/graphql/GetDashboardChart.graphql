query GetDashboardChart($type: DashboardChartType!, $filters: DashboardChartFilters) {
  dashboardChart(type: $type, filters: $filters) {
    labels
    dataPoints {
      label
      values
    }
  }
}
